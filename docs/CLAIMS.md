# Claim ledger

A row is a **measured claim** only when it has a matched baseline and stored artifact.
Accounting and design properties are labelled separately.

## Established / recorded constraints

| # | statement | evidence | status |
|---|---|---|---|
| A1 | Under the declared G1 cost model, active recompilation beats full re-prefill but does not beat DRAM/NVMe KV movement by 512 turns | `benchmarks/kill_gate_crossover.py` | accounting only |
| A2 | Under the declared matched-cost G3 model, full-KV transfer/re-prefill grows 32x from 32K to 1M while fixed-4K active rematerialization stays flat; 1M/2K is cheaper than 32K/16K in the active-state model | `artifacts/g3-mobility-accounting.json` | accounting only |\n| A3 | The predicted crossover is a phase boundary, not a universal win: faster fabrics push it later, larger KV/token pushes it earlier, and smaller active sets push it earlier | `artifacts/g3-phase-space-accounting.json` | accounting only |
| N1 | "text is source / KV is rebuildable artifact" is not sufficient novelty | `docs/NOVELTY.md` | overlap audit |
| N2 | "bounded active KV working set" is not sufficient novelty | `docs/NOVELTY.md` | overlap audit |

## Open measured claims

| # | question | required evidence | gate |
|---|---|---|---|
| Q1 | Is next-turn working state sparse and indexed lookup sublinear on real agent traces, *and* does the retrieved view preserve the turn? | Split, and the split is the finding. **Sparsity: yes, and its shape is worth stating.**  A long session is not a long transcript: five spans hold 73-96% of its tokens and the turn-by-turn content is 4-27% (median 8%) of the footprint (`artifacts/g2-dead-state-v1.json`, `benchmarks/g2_dead_state.py`), so a system whose cost is the footprint retains mostly state that was needed once.  On the 15,000-session public corpus the lexical/provenance index selects a median 12% of a 32K-128K history and its active fraction falls as history grows; on the longest sessions (median history 82,440 tokens, max 156,137) a 4,096-token active view is **5.0%** of history. **Fidelity: not at that size.** Teacher-forced next-turn scoring with a frozen Llama-3.2-1B shows a dose-response to the active budget - token-accuracy delta -10.1 pp (4,096 tokens), -6.0 pp (8,192), -0.26 pp (16,384) in the 8K-32K bucket - and at long histories 4,096 tokens costs -8.3 pp, i.e. the loss is **flat in history** but above the 2 pp tolerance until the view is ~16K tokens, which is 82% of an 8K-32K history. The gate therefore returns `kill` for small fixed budgets and `advance` only when the view approaches the history size.  **Correction:** those three numbers were measured by a renderer that dropped `tool_calls_json` from every message, so neither arm saw the agent's own commands; with that fixed, the 16,384-token view loses **3.46 pp** in the 32K-128K bucket and **32,768 tokens** is what holds (-0.49 pp, active fraction 0.372 at 82K-token histories) - see `artifacts/g2-compiler-recency-b16384-v2.json` and `-b32768-v2.json`. **Collapsing duplicate spans halves that again**: 17.5% of view spans are exact duplicates of another span in the same view, and with dedup a **16,384-token** view holds at -1.94 pp (active fraction 0.200) where the same budget without dedup loses 3.46 pp, while 8,192 with dedup is too small (-6.94 pp) - see `artifacts/g2-compiler-dedup-b16384-v1.json` and `-b8192-v1.json`. **Re-selecting within a span does not help**: keeping the query-relevant lines of an oversized tool dump instead of its prefix scores -6.70 pp at 8,192 against -6.94 pp for plain truncation (within noise), and -13.14 pp at 4,096, so the binding quantity is the volume of distinct content rather than which part of a span is kept (`artifacts/g2-compiler-snippet-b8192-v1.json`, `-b4096-v1.json`).  The compiler is worth more than the budget at long histories: adding recency, oversized-span truncation and identifier provenance to the same index cuts the long-history loss at a fixed 4,096-token view from **-21.98 pp to -7.17 pp** (NLL +1.286 to +0.473) on the same 48 examples, and at 16,384 tokens it reaches **-1.08 pp at an active fraction of 0.199** (median history 82,440 tokens) - within tolerance, and with the fraction four times smaller than the 0.82 the same fidelity needed on 20K-token histories. **Correction, and it moves the gate:** those small-budget losses were the *tail* being cut, not the far field being missing.  Every lexical arm protected only `recency_spans=3` spans and truncated each to `max_span_fraction=0.25` of the budget, so the newest tool results were cut to 2,048 tokens; plain recency at **8,192** tokens - whole newest spans, no retrieval, no compiler - is at parity with full history in every bucket (**+0.29 pp**, NLL -0.048 at 32K-128K, n=44; 0.00 pp at 16,384; +0.66 pp at 32,768), and keeping the newest span whole while spending the remainder on compiled far-field state (`--compile-mode tail_state`) holds 0.00 pp (NLL -0.016).  The fidelity half of the 16K -> 8K gate is therefore met at 8,192, by a view whose active fraction is 0.091 of a 32K-128K history - but teacher-forced fidelity no longer separates a compiler from truncation, so the claim has to be carried by the end task (Q6, Q7).  End-task (not teacher-forced) quality is still required | `artifacts/g2-thoughtworks-structural-v1.json`, `artifacts/g2-model-quality-llama1b-long-v1.json`, `-b8192`, `-b16384`, `-longhist-b4096`, `artifacts/g2-compiler-lexical-b4096-n48.json`, `artifacts/g2-compiler-recency-b4096-n48.json`, `artifacts/g2-compiler-recency-b16384-n48.json` | G2 |
| Q6 | Does bounded retrieval damage the agent's *next* decision, not just the next-token loss? | **Superseded by the bounded-state measurements (kept for the record).**  This row measured the *dedup+snippet compiler's* view, where the end task needed 8,192 tokens; the shipped view - a whole newest window over a consolidated far field - reaches 0.161 at 8,192, 0.150 at 6,144 and, with a 3,584-token window, takes the 4,096-token floor into fidelity tolerance (Section 4.3, `VERDICT.md` C2b/C11).  **Measured then, and it needed half the view.** File-level localization of the generated next turn against the recorded patch's files, 24 paired sessions (median history 32,897 tokens, dedup+snippet compiler, 96 generated tokens): active-budget F1 **0.042 at 4,096** (no better than feeding the whole transcript, 0.045), **0.237 at 8,192** and 0.142 at 16,384, against 0.045 for full history. Paired bootstrap: 4,096 -> 8,192 is **+0.196, 95% CI [+0.024, +0.361], 9 wins / 1 loss**; 8,192 -> 16,384 is +0.095 with a CI containing zero. So the end-task requirement is **~8,192 tokens** where teacher-forced accuracy needs 16,384 - the decision is the more forgiving metric, and it is still four times what the routing gate needs. **Correction: teacher-forced accuracy does not need 16,384.** Keeping the newest spans *whole* puts it at parity at 8,192 (+0.29 pp), so the two metrics disagree rather than differ in size. Arm by arm at 8,192 on the same examples: evidence consolidation without state collapse **0.292**, state-first selection 0.242, raw lexical (dedup + snippet) 0.237, log replay into materialised state 0.231, plain recency 0.154, newest-span-whole plus compiled far field 0.104, newest span *only* plus compiled far field 0.083, full history 0.045. The decision is carried by query-focused evidence and **degraded by replacing the contiguous recent window** with either a compiled far field or a single newest span, which is also what costs the latter 25 pp of fidelity - so the view that buys the decision and the view that buys the surface are still two different
views.  **Correction, and it simplifies the story:** they are not competing for content but for rendering.  In the end-task configuration the window-plus-retrieval view and plain retrieval at 8,192 tokens produce **byte-identical contexts on 48/48 instances** (active tokens equal on all 48; paired decision difference exactly 0.000, 0 wins 0 losses), because retrieval already protects its three newest spans - the window adds no evidence.  Where the newest span exceeds its protection room the rendering differs, so the fidelity differs (0.00 against -6.94 pp) and the decision does not move (0.191 either way).  A system that retrieves the right evidence and renders the newest spans whole gets both at one budget.  **Correction, and it goes against the compiler.**  The favourable decision comparison
(compiler 0.292 against raw 0.237) came from the first 24 qualifying sessions; on one set of 48
paired sessions, full history scores 0.089, plain retrieval **0.243 at 4,096** and 0.149 at 2,048,
and the compiler **0.139 at 8,192** - so plain retrieval at *half* the budget beats the compiler at
the full budget (paired -0.104, 95% CI [-0.208, -0.007], 6 wins / 12 losses).  Per instance the
compiler returns 0.00 where raw returns 1.00 or 0.62 (yamlsettings-36, kombu-1754, dask lm_rewrite),
because supersede-by-identity and snippet selection drop the file the patch touches - the same
destruction the path-collapse ablation saw from the fidelity side (-13.90 pp against -5.44 pp).
The compiler is therefore 1.5 pp better than raw on fidelity and 10 pp worse on the decision, which
is not a trade the paper can call a contribution. **The last inconsistency between those views was then removed and the split held**: the windowed arm's far field had been *dropping* oversized spans (`max_span_fraction=1.0`) while the compiled arm snippet-selects them, so `tail_state` now gives every token after the window the compiled arm's own treatment. Measured at 8,192 with a 50% window: fidelity **0.00 pp** (NLL +0.027) and decision **0.139** - i.e. the trade-off is the budget, not the span treatment. Decision scales with ranked budget (~8K ranked: 0.292; ~4K ranked: 0.139-0.158) and fidelity requires the newest evidence untouched, so at 8,192 the two cannot share and the honest requirement is the smallest budget at which both hold, which is what the 12,288-token arm being measured now is for (receipts: `artifacts/g2b-patch-localization-consolidate-b8192-v1.json`, `artifacts/g2b-patch-localization-tailstate-tf0.6-b8192-v3.json`, `artifacts/g2b-patch-localization-tailquery-cap0.5-b8192-v1.json`, `artifacts/g2b-patch-localization-tailquery-cap0.9-b2048-v1.json`, `artifacts/g2b-patch-localization-recency-b8192-v1.json`) | `artifacts/g2b-patch-localization-b4096-v1.json`, `-b8192-v1.json`, `-b16384-v2.json`, `artifacts/g2b-patch-localization-llama1b-v2.json` | `python benchmarks/g2b_patch_localization.py --jsonl data/sessions-patch-long.jsonl --model <ckpt> --token-budget 8192 --min-history-tokens 32768 --max-length 65536 --max-examples 24 --dedup-spans --snippet-spans --out artifacts/g2b-patch-localization-b8192-v1.json` |
| Q7 | Can history be *compiled* into a smaller sufficient executable view (16K of evidence into 8K of state)? | **The gate splits into two metrics, and the split is the result.** Every arm at an 8,192-token view, both metrics on the same examples: full history scores end-task F1 0.045; **plain recency** (the newest spans kept whole, no retrieval, no compiler) scores **+0.29 pp teacher-forced fidelity** (NLL -0.048) but only 0.154 F1; **evidence consolidation without state collapse** scores **0.292 F1** (the best decision measured) but -5.44 pp fidelity; raw lexical selection scores -6.94 pp / 0.237; log replay into materialised state -7.21 pp / 0.231; state-first selection -15.38 pp / 0.242; and a 16,384-token reference view -1.94 pp / 0.142. Fidelity is bought by the newest span and the decision by query-focused evidence, and no view buys both. **Two corrections follow.** (i) The 5-15 pp 'compiler' losses were not the compiler: every lexical arm protected only `recency_spans=3` spans and truncated each to `max_span_fraction=0.25` of the budget, so the two or three newest tool results - the evidence the next turn's surface form continues - were cut to 2,048 tokens; keeping the newest span whole and spending the remainder on compiled far-field state (`--compile-mode tail_state`) lands at **0.00 pp fidelity (NLL -0.016)**. (ii) Teacher-forced next-turn fidelity therefore cannot distinguish a compiler from truncation on these traces - 8,192 tokens of recency is already at parity - so 'we halve the budget at equal fidelity' is not a defensible headline, and the decision metric is where compilation pays (0.292 against 0.154 for recency, 0.237 for raw and 0.045 for full history). A state compiler has almost nothing to consolidate in any case: over three long examples the retrieved evidence is 74% loose text, 14% command results and **6% file events** | `artifacts/g2-compiler-recency-b8192-v1.json` (and `-b16384`, `-b32768`), `artifacts/g2-compiler-tailstate-tf0.6-b8192-v1.json`, `artifacts/g2b-patch-localization-recency-b8192-v1.json`, `artifacts/g2b-patch-localization-tailstate-tf0.6-b8192-v3.json` (the `-v2` receipt was withdrawn: it predates the fix that stopped the truncation path from rebinding the loop's message text), `artifacts/g2-compiler-consolidate-b8192-nopath-v1.json`, `artifacts/g2b-patch-localization-consolidate-b8192-v1.json`, `artifacts/g2-compiler-statefirst-b8192-v1.json`, `artifacts/g2-compiler-materialize-b8192-v1.json` | `python benchmarks/g2_model_quality.py --jsonl data/sessions-64k.jsonl --model <ckpt> --token-budget 8192 --min-history-tokens 32768 --max-length 131072 --max-examples 48 --compile-mode recency --out artifacts/g2-compiler-recency-b8192-v1.json` |
| Q8 | Where does a history-free mobility cost change the routing decision (G4)? | **174 of 724 cells across the replay grid, 157 of them at active sizes whose measured quality point passes: 77 at the 4,096-token active set (the measured state floor, a 3,584-token window at -0.96 pp), 18 at 6,144 (0.00 pp), 59 at 8,192 and 3 at 16,384 - across all five regimes the replay models: balanced, slow-worker hotspot, worker loss, a flash crowd and a heavy-tailed session-size mix.** Both halves of the cluster gate now clear, in different regimes. Throughput: 117 of the admissible cells clear the 1.5x SLO-goodput bar with a finite ratio (median 1.61x, max 5.06x) and 40 face a strongest baseline that completes *no* work, so their ratio is unbounded rather than large. Tail: **23 cells clear the 30% p99 bar, all of them in the flash-crowd regime** - 8 at 4,096, 8 at 6,144, 6 at 8,192 and 1 at 16,384, best **+68.7%** - because under a fleet-wide burst every worker evicts at once, so a baseline that must move or re-prefill the history queues behind the same spike while 4-8K of rematerialisation does not. The original boundary (4 of 48, forced mobility, 2,048 tokens) was an artefact of the grid: no active size between 2,048 and 16,384, no session beyond 262K, and the measured 12,288 B/token geometry of the only 1M-feasible model on this card (moving 262K tokens of history costs 138 ms there). Scope, stated exactly: the 8,192 column is *fidelity*-admissible (0.00 pp) but its decision is a statistical tie with plain retrieval (+0.081, 95% CI [-0.016, +0.177]), so a strict decision-parity bar clears 0 cells in the whole grid; the balanced and slow-worker wins are the no-cluster-KV-store regime, where the baseline re-prefills (1.6-14.3 s) against 0.203 s of rematerialisation; against a tier that can move KV the wins are worker loss plus the 1M mixes. Every cost is a G3 measurement except the 1M lookup row, now measured on composed million-token histories rather than extrapolated (`g2-index-lookup-composed-1m-v1.json`). The 624 cells measured before the two workload regimes existed reproduce exactly (`paper_numbers.py` asserts it), so the widening is added regimes and a folded-in 6,144 column rather than a re-measurement | `artifacts/g4-quality-join-v3.json`, `artifacts/g4-all-phase-summary-v3.json`, `artifacts/g4b-burst-*-v1.json`, `artifacts/g4b-capacity-*-a6144-*.json` | `python benchmarks/g4_phase_summary.py --glob 'artifacts/g4[b-e]-*.json' --out artifacts/g4-all-phase-summary-v3.json` then `scripts/run_g4_join.sh` |
| Q9 | What does the state bound do to capacity planning? | **Sixteen times the sessions per worker at 128K, two orders of magnitude at 1M.** Derived from measured state sizes (G2: 7-8K tokens, flat in history) and the KV geometry (12,288 B/token measured on Qwen2.5-0.5B; 131,072 and 327,680 declared for 8B/70B classes): with 20 GiB usable HBM per worker, resident *histories* give 13.3 / 1.2 / 0.5 sessions at 128K and 1.7 / 0.2 / 0.1 at 1M, while resident *compiled states* of 8,192 tokens give 213 / 20 / 8 - the full-history object does not fit on real geometries at all.  This is also what makes the G4 capacity-pressure regime concrete: 1-2 resident histories per worker is what an 8B-class fleet with 128K sessions looks like, 16 resident states is what the bound allows | `artifacts/g5-capacity-planning-v1.json`, `benchmarks/g5_capacity_planning.py` | `python benchmarks/g5_capacity_planning.py --out artifacts/g5-capacity-planning-v1.json` |
| Q10 | Does session age still rank the cold-route cost a scheduler pays? | **No, and the ranking is exactly reversed at the extremes.** From the measured primitives: with a 4,096-token state a 1M-token session is the *cheapest* to place cold (0.0954 s) while a 32K session carrying the 16,384-token state that fidelity needed before the bound is the *most expensive* (0.4855 s); the footprint estimate (history x 128 KiB/token / 23.3 GB/s) ranks those two backwards (5.899 s against 0.184 s).  Across bounded-state sessions the true cost is flat at 0.2028-0.2031 s for histories from 32K to 1M, so ranking by history is ranking by a quantity uncorrelated with the cost paid | `artifacts/g6-placement-inversion-v1.json`, `benchmarks/g6_placement_inversion.py` | `python benchmarks/g6_placement_inversion.py --out artifacts/g6-placement-inversion-v1.json` |
| Q2 | Is the cold-route mobility tax <=1.25x from 128K to 1M at fixed active set? | **Measured in parts, with the 1M lookup measured on composed histories.** The ephemeral tax is lookup + active-set prefill: prefill is a function of the active token count only (38.7K / 43.1K / 40.4K / 33.8K tok/s at 2K / 4K / 8K / 16K, repeats=3), and measured lookup is 0.087 / 0.150 / 0.246 ms p50 for <=8K / 8K-32K / 32K-128K histories, i.e. 2.8x growth while the corpus grows ~16x. No trace in the public corpus has 1M-token histories, so that endpoint is postings-growth extrapolation and is labelled as such. **And the state a fixed budget buys does not decay with age**: bucketing the G2 receipts by session length (`artifacts/g2-killer-table-v1.json`) gives a compiled state p50 of 7,829 / 7,899 / 8,203 tokens at raw histories of 64,401 / 82,972 / 155,574 with a teacher-forced fidelity delta of **+0.00 / +0.00 / +1.43 pp** - across a 2.4x growth in history the state grows 5% and the quality delta does not move, so `dM/dL ~ 0` on the fidelity half is measured rather than asserted. **The decision half is now measured at long histories too, and it is a tie there**: on 24 paired instances with 64K-96K histories (p50 65,934), the bounded 8,192-token view scores 0.123 against 0.113 for the full transcript (paired +0.011, 95% CI [-0.129, +0.159], 5 wins / 5 losses), where at ~33K histories the same view beats full history by +0.102.  **And on those long sessions the metric stops separating *any* arm**: bounded retrieval 0.123, the compiler 0.111, the full transcript 0.113 (raw against compiled: -0.012, 95% CI [-0.131, +0.120], 2 wins / 5 losses).  The honest reading is that the bounded state is never *worse* than full history and is clearly better at moderate lengths, while the two converge on the longest sessions in this corpus - and the fidelity side of the table stays flat across the same lengths (+0.00 / +0.00 / +1.43 pp at 64K / 83K / 156K), and on the turn axis it reads -1.43 / +0.00 / +0.00 pp at 40 / 58 / 96 turns (`artifacts/g2-killer-table-v4.json`).  The original single-length note follows: every end-task receipt comes from ~32.9K-token histories (the filter takes the first examples above the threshold), and the 338 corpus sessions at 64K-128K are being measured | `artifacts/g3-active-prefill-warm-qwen05b-v1.json`, `artifacts/g3-hardware-primitives-qwen05b-v1.json`, `artifacts/g2-thoughtworks-structural-v1.json` | G3 |
| Q3 | Can 1M/2K be cheaper to move than 32K/16K on hardware? | **Yes, measured.** 1M history with a 2K active set: ~0.10 s (0.053 s prefill + lookup) against 32K history with a 16K active set: ~0.49 s, a 5x separation set by the active set rather than the history. Against a full-KV move on this local 23.2-23.4 GB/s link, the 1M KV payload (12.00 GiB) costs 555.6 ms, so 1M/2K is 5.6x cheaper than moving it and 1M/16K is not; the crossover sits near 120 GB/s effective bandwidth. Composed from the G3 primitives with the compiled states G2 measured, the inversion is explicit: 1M history with an 8,192-token compiled state costs 0.2031 s and with 4,096 costs 0.0954 s, against 0.4855 s for a 32K history carrying the 16,384-token raw view fidelity needed before the compiler - i.e. a session **32x older is 2.4x (5.1x at 4K) cheaper to move**, and the full-KV move only becomes the better cold route near 262K-1M tokens on a 23.3 GB/s link (`artifacts/g2-killer-table-v1.json`, `inversion`) | `artifacts/g3-hardware-primitives-qwen05b-v1.json`, `artifacts/g3-fullprefill-qwen05b-v1.json` | G3 |
| Q4 | Does replacing history-sized cold-route cost with measured active-set cost change the best routing decision under realistic skew/failure? | **Yes, and now on both halves of the cluster gate.** With measured G3 costs and a declared workload, the admissible region is **157 of 724 cells** across all five regimes (`g4-quality-join-v3.json`, Q8): 117 clear the 1.5x SLO-goodput bar, 40 face a baseline that completes no work, and **23 clear the 30% p99 bar - all of them in the flash-crowd regime**, where a fleet-wide arrival burst evicts warm state on every worker at once (best +68.7%).  The first answer to this question was **4 of 48 cells, worker loss only, 2,048 tokens** (`artifacts/g4-phase-summary-v1.json`), and it was an artefact of a grid with no active size between 2,048 and 16,384, no session past 262K and only worker-behaviour regimes.  The 16,384-token column is where the old grid's reading came from: at that active set a full-KV move on a 23.2-23.4 GB/s fabric costs 0.017-0.556 s against 0.485 s of rematerialisation, so the column closes (3 of 82 cells) - and the state the paper actually runs at is 4-8K, not 16K | `artifacts/g4-quality-join-v3.json`, `artifacts/g4-all-phase-summary-v3.json`, `artifacts/g4-phase-summary-v1.json` | G4 |
| Q5 | Can a worker or a model be replaced without transferring history-sized state? | **Measured, first receipt.** With a median 130,131-token history, a model rollout resumes from the durable transcript + index for 0.25 ms of lookup plus **0.55 s** (Qwen2.5-0.5B) or **1.04 s** (Llama-3.2-1B) of active-set prefill on the *new* model, against **6.31 s** to re-prefill the history (optimistic linear scaling; infeasible at 512K+) - and the same durable view keeps the next turn's fidelity on a model that never saw the session (Llama-3.2-1B: token-accuracy delta +0.48 pp, NLL -0.010). **End-task success is now measured on models that never saw the session**: rebuilding a 4,096-token state from the durable index scores end-task F1 0.137 against 0.017 for the whole transcript on Qwen2.5-0.5B (+0.120) and 0.145 against 0.042 on Qwen2.5-1.5B (+0.104); a 16,384-token view gives 0.196 against 0.017 on the 0.5B model.  So recovery on a foreign model keeps the decision, which is what makes rollout and failover the same operation.  **And the process-level failover is now measured**: worker A serves a turn and is SIGKILLed (its KV dies with it), then a *fresh process* rebuilds 8,192 tokens of state from the durable object in 0.91-1.42 s (p50 1.387 s), naming the recorded files in 4 of 6 sessions, with teacher-forced token accuracy identical to the killed owner's (0.593 both); it reads 32,455 bytes of state text where the dissolved owner held 0.38 GiB of KV even at these 33K histories.  What is still open is a *fleet-level* scheduler re-placing the session - the G4 replay models that, no live cluster does it | `artifacts/g5-rollout-v1.json` | G5 |

Q1 is measured and split, Q2/Q3 have hardware receipts (the 1M lookup is measured on composed million-token histories and labelled as composition), and Q4 is measured and conditional; Q5 has a first receipt (composed costs plus cross-model fidelity; end-task and real failover still open).  None of the receipts is an end-task result.
